// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#F7C366',
    },
    
    secondary: {
      main: '#5552E7',
    },
    tertiary: {
      main: '#008F7E', 
    },

  },
});

export default theme;
